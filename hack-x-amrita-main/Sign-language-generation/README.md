
# AI-based STEM Sign - Frontend (Scaffold)

This repository contains a minimal React + Tailwind CSS frontend scaffold for the "AI-based STEM Sign" learning platform.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Start dev server

```bash
npm run dev
```

What is included:
- Basic Vite-compatible `index.html` and `src` entry
- `Navbar` and responsive `Sidebar` with routing
- Pages: Home, Topics, Generate, Saved, About
- Tailwind CSS config with dark mode (class-based)
- Light / Dark toggle that persists choice in `localStorage`

Next steps (suggested):
- Wire up design tokens and components
- Add real content and integrate backend APIs
- Add tests and CI

# Sign-language-generation

